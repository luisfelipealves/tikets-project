apiVersion: v1
kind: Service
metadata:
  name: event-service-loadbalancer
spec:
  type: LoadBalancer 
  selector:
    app: event-service 
  ports:
    - protocol: TCP
      port: 80      
      targetPort: 8080 